{
  "name": "autonav",
  "version": "0.1.0",
  "private": true,
  "description": "LLM-agnostic context management system for autonomous navigators",
  "workspaces": [
    "packages/*",
    "packs/*"
  ],
  "scripts": {
    "build": "npm run build -w @autonav/communication-layer && npm run build -w @autonav/core",
    "test": "npm test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/build packs/*/dist",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present"
  },
  "keywords": [
    "autonav",
    "navigator",
    "knowledge-management",
    "claude",
    "agent-sdk",
    "ai"
  ],
  "author": "Terra",
  "license": "TBD",
  "devDependencies": {
    "@types/marked": "^5.0.2",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
